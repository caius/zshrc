# function savegif {
  # If arg 2 is given, use that as filename
  if [[ -n $2 ]]; then
    destination="$2"
  else
    # Otherwise, use the gif's name from the URL
    destination="$(basename "$1")"
  fi
  curl -o "$HOME/GoogleDrive/GifCentral/${destination/.gif/}.gif" "$1"
# }
