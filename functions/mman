# Open manpages in TextMate
# Runs the command in a subshell so we don't change $PWD
# function mman () {
  $(cd $HOME && "$HOME/Library/Application Support/TextMate/Managed/Bundles/Man Pages.tmbundle/Support/mman" $*)
# }
